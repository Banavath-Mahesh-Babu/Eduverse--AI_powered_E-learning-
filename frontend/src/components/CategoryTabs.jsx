import React from 'react';export default function CategoryTabs({value,onChange}){const t=[{key:'all',label:'All'},{key:'literature',label:'Literature'},{key:'business',label:'Business Studies'},{key:'computers',label:'Computer Science'}];return(<div className='flex flex-wrap gap-2'>{t.map(x=>(<button key={x.key} onClick={()=>onChange(x.key)} className={`px-3 py-1.5 rounded-full text-sm border ${value===x.key?'bg-black text-white border-black':'bg-white border-gray-300 hover:bg-gray-50'}`}>{x.label}</button>))}</div>)}